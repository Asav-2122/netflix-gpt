import React from 'react'
import { Outlet } from 'react-router-dom'

export const DefaultLayOut = () => {
    return (
        <div>

            DefaultLayOut


            <Outlet />
        </div>
    )
}
